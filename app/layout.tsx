export const metadata = {
  title: 'Studio App',
  description: 'Firebase App Hosting',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
